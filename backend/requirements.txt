# --- API layer ---------------------------------------------------------------
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.8
python-multipart>=0.0.9

# --- persistence (MongoDB Atlas) ---------------------------------------------
pymongo>=4.8

# --- agent pipeline ----------------------------------------------------------
# The backend executes the five-agent graph in `agents/`, so it owns these.
langgraph>=0.2.28
langchain-core>=0.3

# --- auth --------------------------------------------------------------------
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7.4

# --- documents & reporting ---------------------------------------------------
pymupdf>=1.24
python-docx>=1.1
reportlab>=4.2

# --- outbound calls (Gemini provider) ----------------------------------------
httpx>=0.27

# --- MCP ---------------------------------------------------------------------
mcp>=1.2

# --- tests -------------------------------------------------------------------
pytest>=8.0
pytest-asyncio>=0.23
